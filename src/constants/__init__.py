from pathlib import Path

# Path to the main configuration YAML file
CONFIG_FILE_PATH = Path("config/config.yaml")

# Path to the model training hyperparameters YAML file
PARAMS_FILE_PATH = Path("params.yaml")
